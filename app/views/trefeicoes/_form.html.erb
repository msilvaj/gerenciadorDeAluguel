<%= form_with(model: trefeicao, local: true) do |form| %>
  <% if trefeicao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trefeicao.errors.count, "error") %> prohibited this trefeicao from being saved:</h2>

      <ul>
      <% trefeicao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
